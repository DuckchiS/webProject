<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.web.mapper.BoardMapper">
	<select id="getCount" resultType="int" parameterType="map">
		select count(*) from board where b_category = #{category}
	</select>
	
	<select id="getSearchCount" resultType="int" parameterType="map">
	    select count(*) from board where b_category = #{category} and b_title like concat('%',#{word},'%')
	</select>
	
	<select id="getList" resultType="com.example.web.dto.BoardDto" parameterType="map">
		select * from board where b_category = #{category} limit #{limitIndex},5
	</select>
	
	<select id="getSearchList" resultType="com.example.web.dto.BoardDto" parameterType="map">
    	select * from board where b_category = #{category} and b_title like concat('%',#{word},'%') limit #{limitIndex}, 5
	</select>
	
	<select id="read" resultType="com.example.web.dto.BoardDto">
		select * from board where b_no = #{b_no}
	</select>
	
	<select id="getReplys" resultType="com.example.web.dto.ReplyDto">
		select * from reply where b_no = #{b_no}
	</select>
	
	<delete id="del">
		delete from board where b_no = #{b_no}
	</delete>
	
	<delete id="replyDel">
		delete from reply where b_no = #{b_no}
	</delete>
	
	<insert id="write">
		insert into board (b_title,b_content,m_nickname,b_category) values (#{b_title},#{b_content},#{m_nickname},#{b_category}) 
	</insert>
	
	<insert id="reply">
		insert into reply (r_content,m_nickname,b_no) value(#{r_content},#{m_nickname},#{b_no})
	</insert>
	
	<update id="modify">
		update board set b_content = #{b_content}, b_updatetime = now(), b_category = #{b_category} where b_no = #{b_no}
	</update>
	
	<update id="hit">
		update board set b_hit = b_hit + 1 where b_no = #{b_no}
	</update>
	
	<update id="like">
		update board set b_like = b_like + 1 where b_no = #{b_no}
	</update>
</mapper>